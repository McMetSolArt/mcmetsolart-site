# Example .env for McMetSolArtBackend
# Copy to .env and set values for production

# Flask debug mode (True/False)
FLASK_DEBUG=False

# Host and port
HOST=0.0.0.0
PORT=5000

# SQLite database file or full path
DATABASE=mc_metsolart.db

# Enable real emails (False by default)
EMAIL_ENABLED=False
# If EMAIL_ENABLED=True, set your SMTP/SendGrid settings in config_email.py or via env

# Optional: administrative email
ADMIN_EMAIL=mc_metsolart@yahoo.com
