╔═══════════════════════════════════════════════════════════════════════════════╗
║                         INFORMAȚII DUPĂ DEPLOY                                ║
║                          Ce se va întâmpla pe Render                           ║
╚═══════════════════════════════════════════════════════════════════════════════╝

IMEDIAT DUPĂ DEPLOY
═══════════════════════════════════════════════════════════════════════════════

1. INIȚIALIZARE DATABASE
   - start.sh rulează migrate_add_api_token.py
   - Se crează database.db în /tmp
   - Se adaugă colonă api_token la tabela users
   - Timp: ~5 secunde

2. START GUNICORN SERVER
   - Backend pornește pe port 8000
   - Frontend static files sunt servite automat
   - URL: https://mc-metsolart.onrender.com

3. HEALTH CHECK
   - Render verific dacă app răspunde
   - Dacă merge → Status: LIVE ✅


FUNCȚIONALITĂȚI ACTIVATE
═══════════════════════════════════════════════════════════════════════════════

API ENDPOINTS:
  ✅ POST /api/auth/register - Register user
  ✅ POST /api/auth/login - Login
  ✅ GET /api/auth/profile - Get user profile
  ✅ POST /api/orders - Create order
  ✅ GET /api/orders - Get user orders
  ✅ POST /api/support - Send support message
  ✅ GET /api/health - Health check

FRONTEND:
  ✅ Login/Register
  ✅ Product viewing
  ✅ Contact form
  ✅ Account management
  ✅ Order tracking (dacă sunt ordine în DB)

DATABASE:
  ✅ Users table cu permanent API tokens
  ✅ Orders table
  ✅ Notifications, Shipping addresses, etc.


IMPORTANT: FREE TIER LIMITĂRI
═══════════════════════════════════════════════════════════════════════════════

1. AUTO-SLEEP
   - După 15 minute inactivitate, app-ul "dormě"
   - Prima cerere după sleep = ~30 sec delay
   - SOLUȚIE: Render PRO tier (cu plată)

2. STORAGE
   - ~15GB disponibil pentru database
   - SQLite pe /tmp se șterge la fiecare redeploy
   - SOLUȚIE PRODUCȚIE: PostgreSQL (free pe Render cu limitări)

3. BUILD TIME
   - Max 30 minute per build
   - Dacă depășești → build fail
   - E suficient pentru package-ul nostru

4. BANDWIDTH
   - Free tier are limită dar e generos
   - OK pentru traffic normal


ACCESARE WEBSITE
═══════════════════════════════════════════════════════════════════════════════

URL public va fi:
  https://YOUR_SERVICE_NAME.onrender.com

Exemplu (dacă service name e "mc-metsolart"):
  https://mc-metsolart.onrender.com

Copiază din Render Dashboard → Settings → Render URL


MONITORARE
═══════════════════════════════════════════════════════════════════════════════

1. LOGS
   - Render Dashboard → Logs
   - Vezi orice erori sau mesaje
   - Refresh automat

2. METRICS
   - CPU, Memory usage
   - Dashboard → Metrics

3. ALERTS
   - Render trimite notificări dacă e problema
   - Check email-ul de pe cont


DATABASE MANAGEMENT
═══════════════════════════════════════════════════════════════════════════════

SQLite pe /tmp:
  - Fișier temporar
  - Se șterge la fiecare redeploy
  - OK pentru testing

Pentru PRODUCȚIE:
  - Setup PostgreSQL pe Render (free cu limitări)
  - Modifică app.py pentru PostgreSQL
  - Connection string din Render environment


ACTUALIZĂRI & MAINTENANCE
═══════════════════════════════════════════════════════════════════════════════

Redeploy oricând:
  1. Modifică cod local
  2. git push
  3. Render redeploy automat
  4. Rebuild + Restart (cpl 5 min)

Stop/Restart service:
  - Render Dashboard → Settings → Suspend/Restart


TROUBLESHOOTING DEPLOY
═══════════════════════════════════════════════════════════════════════════════

PROBLEMA: Build failed
SOLUȚIE:
  - Verific Logs pentru detalii
  - Caz obișnuit: missing import sau syntax error
  - Fix + git push = re-trigger build

PROBLEMA: Service won't start
SOLUȚIE:
  - Verific Logs → Runtime errors
  - Check start.sh - cale corecte?
  - Check app.py - syntax ok?

PROBLEMA: Site încarcă dar API errors
SOLUȚIE:
  - Logs: verific API errors
  - Check database initialization
  - Verific environment variables în Render Dashboard

PROBLEMA: Static files (CSS/JS) nu se încarcă
SOLUȚIE:
  - Logs: 404 errors?
  - Verific app.py - has static routes?
  - Rebuild + redeploy

PROBLEMA: Slow after inactivity
SOLUȚIE:
  - E normal (free tier auto-sleep)
  - Prima request după inactivitate = slow
  - Upgrade la PRO dacă vrei instant


CONFIGURARE AVANSAT
═══════════════════════════════════════════════════════════════════════════════

Email notifications:
  - Render → Settings → Notifications
  - Setup alerts pe email

Custom domain:
  - Render → Settings → Custom Domains
  - Add your domain
  - Configure DNS

SSL/HTTPS:
  - Automat pe subdomeniu Render
  - Automat pe custom domain (Let's Encrypt)

Environment secrets:
  - Render Dashboard → Environment
  - Add/modify variables
  - Auto-redeploy daca schimbi


PERFORMANCE TIPS
═══════════════════════════════════════════════════════════════════════════════

1. Database:
   - Upgrade la PostgreSQL dacă SQLite e lent
   - Add indexes pe frequently queried columns

2. Frontend:
   - Minify CSS/JS dacă posibil
   - Cache busting pe images

3. Backend:
   - Enable Gunicorn worker scaling
   - render.yaml → adjust workers

4. Monitoring:
   - Verific logs periodic
   - Alertă pe errors


UPGRADE OPTIONS
═══════════════════════════════════════════════════════════════════════════════

Dacă free tier nu e suficient:

✅ STARTER ($7/luna):
   - No auto-sleep
   - 1 instance
   - Basic metrics

✅ PRO ($12/luna):
   - More power
   - Multiple instances
   - Priority support

PostgreSQL:
   - Free cu limitări (5 connecții)
   - STARTER ($7/luna) - 20 connecții

Setup: Render Dashboard → Settings → Plan


BACKUP & RESTORE
═══════════════════════════════════════════════════════════════════════════════

GitHub = Backup:
  - Orice modificare push-ezi pe GitHub
  - E backing up automat

Database:
  - Free tier: SQLite în /tmp (nu persistent)
  - Pentru production: PostgreSQL (persistent)

Export data:
  - Descarcă SQLite file din runtime
  - Sau: Export din PostgreSQL

═══════════════════════════════════════════════════════════════════════════════

✅ GATA! Site-ul tău e LIVE! 🎉

Oricând modifici cod, doar git push și Render se ocupă de rest.

═══════════════════════════════════════════════════════════════════════════════
